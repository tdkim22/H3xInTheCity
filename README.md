# H3x and the City
Using the processed data from water usage metrics to make generalizations of areas and groups.

## Setup Instructions
1. Make sure all packages in `requirements.txt` are installed using ```python
   pip install -r requirements.txt ```
3. Provided all naming conventions are followed, file paths should not need to be changed. Files in `output` and `BlacksburgZoningData` are imported into some scripts and must be loaded into the workspace prior to running the code.
4.  
